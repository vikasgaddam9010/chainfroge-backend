"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkSMTP = void 0;
const net_1 = __importDefault(require("net"));
const output_1 = require("../output/output");
const errorCodes_1 = require("./errorCodes");
const log = (...args) => {
    if (process.env.DEBUG === 'true') {
        console.log(...args);
    }
};
exports.checkSMTP = (sender, recipient, exchange, helloName, timeout = 10000) => __awaiter(void 0, void 0, void 0, function* () {
    // const timeout = 1000 * 10; // 10 seconds
    return new Promise(r => {
        let receivedData = false;
        const socket = net_1.default.createConnection(25, exchange);
        socket.setEncoding('ascii');
        socket.setTimeout(timeout);
        socket.on('error', error => {
            log('error', error);
            socket.emit('fail', error);
        });
        socket.on('close', hadError => {
            if (!receivedData && !hadError) {
                socket.emit('fail', 'Mail server closed connection without sending any data.');
            }
        });
        socket.once('fail', (strOrError) => {
            var _a;
            let code;
            let reason;
            if (strOrError instanceof Error) {
                reason = JSON.stringify(strOrError);
                // eslint-disable-next-line @typescript-eslint/ban-ts-ignore
                // @ts-ignore
                if (strOrError['code'] === 'ENOTFOUND') {
                    code = 404;
                }
            }
            else {
                code = (_a = Object.typedKeys(errorCodes_1.ErrorCodes).filter(x => errorCodes_1.hasCode(strOrError, x))) === null || _a === void 0 ? void 0 : _a[0];
                reason = strOrError;
            }
            r(output_1.createOutput('smtp', reason, code));
            if (socket.writable && !socket.destroyed) {
                socket.write(`quit\r\n`);
                socket.end();
                socket.destroy();
            }
        });
        socket.on('success', () => {
            if (socket.writable && !socket.destroyed) {
                socket.write(`quit\r\n`);
                socket.end();
                socket.destroy();
            }
            r(output_1.createOutput());
        });
        // const commands = [`EHLO ${helloName}\r\n`, `MAIL FROM: <${sender}>\r\n`, `RCPT TO: <${recipient}>\r\n`];
        const commands = [`EHLO ${helloName}\r\n`, `MAIL FROM: <${recipient}>\r\n`, `RCPT TO: <${recipient}>\r\n`];
        let i = 0;
        socket.on('next', () => {
            if (i < 3) {
                if (socket.writable) {
                    socket.write(commands[i++]);
                }
                else {
                    socket.emit('fail', 'SMTP communication unexpectedly closed.');
                }
            }
            else {
                socket.emit('success');
            }
        });
        socket.on('timeout', () => {
            socket.emit('fail', 'Timeout');
        });
        socket.on('connect', () => {
            socket.on('data', msg => {
                receivedData = true;
                log('data', msg);
                console.log(`msg :: ${msg}`)
                if (errorCodes_1.hasCode(msg, 220) || errorCodes_1.hasCode(msg, 250)) {
                    socket.emit('next', msg);
                }
                else if (errorCodes_1.hasCode(msg, 550)) {
                    socket.emit('fail', 'Mailbox not found.');
                }
                else {
                    socket.emit('fail', msg);
                }
            });
        });
    });
});
//# sourceMappingURL=smtp.js.map